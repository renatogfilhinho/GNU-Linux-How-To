######## algumas formas de se usar o comando for:

------ Loop for com uma lista de valores:

# Exemplo 1
for animal in gato cachorro pássaro; do
    echo "O animal é: $animal"
done

# Exemplo 2
for cor in vermelho verde azul; do
    echo "A cor é: $cor"
done

------ Loop for com uma sequência numérica:

# Exemplo 1
for ((i=1; i<=5; i++)); do
    echo "Contagem: $i"
done

# Exemplo 2
for ((j=10; j>=0; j--)); do
    echo "Decrescente: $j"
done

------ Loop for usando um comando substituto:

for arquivo in $(ls); do
    echo "Arquivo encontrado: $arquivo"
done

# Exemplo 2
for palavra in $(grep -o "OpenAI" README.md); do
    echo "Palavra encontrada: $palavra"
done
